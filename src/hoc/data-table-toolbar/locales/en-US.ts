import type { DataTableToolbarLocale } from '@/i18n/types'

export const enUS: DataTableToolbarLocale = {
  searchPlaceholder: 'Search...',
  columns: 'Columns',
  toggleAll: 'Toggle all',
  resetFilters: 'Reset',
}
