---
title: Tabs
navTitle: Tabs
description: Accessible tab list, triggers, indicator, and panels built on Kobalte.
section: Components
toc: true
---

import TabsDemo from "../../../examples/tabs-demo"
import TabsOrientationDemo from "../../../examples/tabs-orientation-demo"

Tabs use Kobalte’s tabs primitive with orientation-aware layout, a sliding indicator, and theme hooks for list styling. Behavior (value, controlled mode, keyboard navigation) matches Kobalte’s API.

## Usage

```tsx
import {
  Tabs,
  TabsContent,
  TabsIndicator,
  TabsList,
  TabsTrigger,
} from "shadcn-solid-components/components/tabs"
```

Place `TabsTrigger` elements inside `TabsList` (with `TabsIndicator` as needed), and pair each trigger with a `TabsContent` using the same `value`. See [Kobalte Tabs](https://kobalte.dev/docs/core/components/tabs) for props such as `value`, `onChange`, and `orientation`.

## Exports

- `Tabs`, `TabsList`, `TabsTrigger`, `TabsIndicator`, `TabsContent`
- Types: `TabsProps`, `TabsListProps`, `TabsTriggerProps`, `TabsIndicatorProps`, `TabsContentProps`

## Examples

### Default

<ComponentPreview name="Tabs Demo" source="examples/tabs-demo.tsx">
  <div class="mb-4 w-full">
    <TabsDemo />
  </div>
</ComponentPreview>

### Orientation

<ComponentPreview name="Tabs Orientation Demo" source="examples/tabs-orientation-demo.tsx">
  <div class="mb-4 w-full">
    <TabsOrientationDemo />
  </div>
</ComponentPreview>
