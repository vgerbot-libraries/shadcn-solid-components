---
title: Alert
description: Inline message for status, feedback, or important information.
section: Components
toc: true
---

import AlertDemo from "../../../examples/alert-demo"
import AlertDestructiveDemo from "../../../examples/alert-destructive-demo"
import AlertStaticDemo from "../../../examples/alert-static-demo"

Use `Alert` for inline, persistent feedback inside a page or section. Combine it with `AlertTitle` and `AlertDescription` when you need more structure than plain text.

## Usage

```tsx
import { Alert, AlertDescription, AlertTitle } from "shadcn-solid-components/components/alert"
```

```tsx
<Alert as="div">
  <AlertTitle>Heads up!</AlertTitle>
  <AlertDescription>
    Your trial ends in 3 days.
  </AlertDescription>
</Alert>
```

## Accessibility And Semantics

`Alert` is built on Kobalte's alert primitive and defaults to rendering as a `button`.

Use `as="div"` when the alert is informational and should not be interactive. Keep the default element, or render another interactive element, only when the entire alert is meant to be clicked.

## Variants

- `default`: General information, neutral status, or success-style messaging.
- `destructive`: Error, warning, or destructive state that needs stronger emphasis.

## Custom Styling

Import `alertVariants` when you want to reuse the same visual treatment on a custom wrapper or extend the built-in variants.

```tsx
import { alertVariants } from "shadcn-solid-components/components/alert"
```

## Examples

### Default

<ComponentPreview name="Alert Default" source="examples/alert-demo.tsx">
  <div class="mb-4 w-full max-w-xl">
    <AlertDemo />
  </div>
</ComponentPreview>

### Static Informational Alert

<ComponentPreview name="Alert Static" source="examples/alert-static-demo.tsx">
  <div class="mb-4 w-full max-w-xl">
    <AlertStaticDemo />
  </div>
</ComponentPreview>

### Destructive

<ComponentPreview name="Alert Destructive" source="examples/alert-destructive-demo.tsx">
  <div class="mb-4 w-full max-w-xl">
    <AlertDestructiveDemo />
  </div>
</ComponentPreview>

## Exports

- `Alert`, `AlertTitle`, `AlertDescription`, `alertVariants`
- Types: `AlertProps`, `AlertTitleProps`, `AlertDescriptionProps`
