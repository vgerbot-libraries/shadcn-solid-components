import type { ImagePickerLocale } from 'shadcn-solid-components/i18n/types'

export const enUS: ImagePickerLocale = {
  upload: 'Upload image',
  cameraUpload: 'Take photo',
  remove: 'Remove image',
}
