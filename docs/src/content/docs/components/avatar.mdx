---
title: Avatar
description: User avatar primitives with image fallback and stacked avatar groups.
section: Components
toc: true
---

import AvatarDemo from "../../../examples/avatar-demo"
import AvatarGroupDemo from "../../../examples/avatar-group-demo"

`Avatar` provides composable image/fallback slots for profile photos. Use `AvatarGroup` to stack multiple users and optionally show an overflow counter with `max` + `total`.

## Usage

```tsx
import {
  Avatar,
  AvatarFallback,
  AvatarGroup,
  AvatarImage,
} from "shadcn-solid-components/components/avatar"
```

```tsx
<Avatar src="https://github.com/shadcn.png">
  <AvatarImage alt="Shadcn" />
  <AvatarFallback>SC</AvatarFallback>
</Avatar>
```

## Exports

- `Avatar`, `AvatarImage`, `AvatarFallback`, `AvatarGroup`
- Types: `AvatarProps`, `AvatarImageProps`, `AvatarFallbackProps`, `AvatarGroupProps`

## Examples

### Avatar with image and fallback

<ComponentPreview name="Avatar Demo" source="examples/avatar-demo.tsx">
  <div class="mb-4 w-full">
    <AvatarDemo />
  </div>
</ComponentPreview>

### Stacked avatar group

<ComponentPreview name="Avatar Group Demo" source="examples/avatar-group-demo.tsx">
  <div class="mb-4 w-full">
    <AvatarGroupDemo />
  </div>
</ComponentPreview>
