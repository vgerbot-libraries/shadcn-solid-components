---
title: Dialog
description: Modal overlay for focused tasks or information.
section: Components
toc: true
---

import DialogDemo from "../../../examples/dialog-demo"

Dialogs use Kobalte’s dialog primitive with portal, overlay, content, header/footer regions, title, description, trigger, and an explicit close button part.

## Usage

```tsx
import {
  Dialog,
  DialogCloseButton,
  DialogContent,
  DialogDescription,
  DialogFooter,
  DialogHeader,
  DialogPortal,
  DialogTitle,
  DialogTrigger,
} from "shadcn-solid-components/components/dialog"
```

## Exports

- `Dialog`, `DialogPortal`, `DialogTrigger`, `DialogCloseButton`, `DialogContent`, `DialogHeader`, `DialogFooter`, `DialogTitle`, `DialogDescription`
- Types: `DialogProps`, `DialogTriggerProps`, `DialogCloseButtonProps`, `DialogContentProps`, `DialogHeaderProps`, `DialogFooterProps`, `DialogTitleProps`, `DialogDescriptionProps`

## Examples

### Default

<ComponentPreview name="Dialog Demo" source="examples/dialog-demo.tsx">
  <div class="mb-4 w-full">
    <DialogDemo />
  </div>
</ComponentPreview>
