---
title: Alert Dialog
navTitle: Alert Dialog
description: Modal dialog for critical confirmations that interrupt the user flow.
section: Components
toc: true
---

import AlertDialogDemo from "../../../examples/alert-dialog-demo"

Alert dialogs present blocking confirmation UI: overlay, title, description, and primary/secondary actions wired to Kobalte’s alert dialog primitive.

## Usage

```tsx
import {
  AlertDialog,
  AlertDialogAction,
  AlertDialogCancel,
  AlertDialogContent,
  AlertDialogDescription,
  AlertDialogFooter,
  AlertDialogHeader,
  AlertDialogOverlay,
  AlertDialogPortal,
  AlertDialogTitle,
  AlertDialogTrigger,
} from "shadcn-solid-components/components/alert-dialog"
```

Use `AlertDialogTrigger` to open, `AlertDialogContent` for the modal body, and `AlertDialogAction` / `AlertDialogCancel` as close buttons with appropriate button styles.

## Exports

- `AlertDialog`, `AlertDialogPortal`, `AlertDialogTrigger`, `AlertDialogOverlay`, `AlertDialogContent`, `AlertDialogHeader`, `AlertDialogFooter`, `AlertDialogTitle`, `AlertDialogDescription`, `AlertDialogAction`, `AlertDialogCancel`
- Types: `AlertDialogProps`, `AlertDialogTriggerProps`, `AlertDialogOverlayProps`, `AlertDialogContentProps`, `AlertDialogHeaderProps`, `AlertDialogFooterProps`, `AlertDialogTitleProps`, `AlertDialogDescriptionProps`, `AlertDialogActionProps`, `AlertDialogCancelProps`

## Examples

### Default

<ComponentPreview name="Alert Dialog Demo" source="examples/alert-dialog-demo.tsx">
  <div class="mb-4 w-full">
    <AlertDialogDemo />
  </div>
</ComponentPreview>
