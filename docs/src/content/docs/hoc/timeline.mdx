---
title: Timeline
description: Vertical timeline for activity feeds with left, right, and alternate layouts, custom icons, color accents, and a pending-tail indicator.
section: HOC
toc: true
---

import TimelineLeftDemo from "../../../examples/timeline-left-demo"
import TimelineRightDemo from "../../../examples/timeline-right-demo"
import TimelineAlternateDemo from "../../../examples/timeline-alternate-demo"
import TimelinePendingDemo from "../../../examples/timeline-pending-demo"
import TimelineCustomIconDemo from "../../../examples/timeline-custom-icon-demo"
import TimelineOrderTrackingDemo from "../../../examples/timeline-order-tracking-demo"

`Timeline` renders a vertical list of events connected by a line. Each event is a **node** (dot or custom icon) paired with a **content block** (title, description, time, and optional rich content).

Three layout modes are available:

- **`left`** (default) — all events aligned to the left.
- **`right`** — all events aligned to the right.
- **`alternate`** — events alternate left and right on medium+ screens; collapses to left-aligned on mobile.

When `pending` is `true`, a pulsing node and a localized "Loading…" label are appended after the last item, useful for streams that are still in progress.

## Usage

```tsx
import { Timeline } from "shadcn-solid-components/hoc/timeline"

<Timeline
  mode="left"
  items={[
    { title: "Created", time: "10:00", description: "Order placed." },
    { title: "Shipped", color: "text-green-500" },
  ]}
/>
```

## Examples

### Left layout

The default mode. All events appear on the left side with the timeline line running down the left edge.

<ComponentPreview name="Timeline Left Demo" source="examples/timeline-left-demo.tsx">
  <div class="mb-4 w-full max-w-lg">
    <TimelineLeftDemo />
  </div>
</ComponentPreview>

### Right layout

All events are right-aligned. Useful when the timeline sits on the right side of a two-column layout.

<ComponentPreview name="Timeline Right Demo" source="examples/timeline-right-demo.tsx">
  <div class="mb-4 w-full max-w-lg">
    <TimelineRightDemo />
  </div>
</ComponentPreview>

### Alternate layout

Events alternate sides on medium+ screens. Each item can include `content` for rich sub-content like links or cards.

<ComponentPreview name="Timeline Alternate Demo" source="examples/timeline-alternate-demo.tsx">
  <div class="mb-4 w-full max-w-2xl">
    <TimelineAlternateDemo />
  </div>
</ComponentPreview>

### Pending tail

Set `pending` to append a pulsing indicator after the last item. In non-alternate modes a localized label ("Loading…") is shown beside the dot.

<ComponentPreview name="Timeline Pending Demo" source="examples/timeline-pending-demo.tsx">
  <div class="mb-4 w-full max-w-lg">
    <TimelinePendingDemo />
  </div>
</ComponentPreview>

### Custom icons and colors

Each item can override the default dot with a custom `icon` and apply a Tailwind text-colour class via `color`.

<ComponentPreview name="Timeline Custom Icon Demo" source="examples/timeline-custom-icon-demo.tsx">
  <div class="mb-4 w-full max-w-lg">
    <TimelineCustomIconDemo />
  </div>
</ComponentPreview>

### Order tracking

A real-world pattern combining `pending`, custom icons, `color`, and `content` for a shipment tracker.

<ComponentPreview name="Timeline Order Tracking Demo" source="examples/timeline-order-tracking-demo.tsx">
  <div class="mb-4 w-full max-w-lg">
    <TimelineOrderTrackingDemo />
  </div>
</ComponentPreview>

## API

### TimelineProps

| Prop | Type | Default | Description |
| --- | --- | --- | --- |
| `items` | `TimelineItem[]` | — | The events to display. **Required.** |
| `mode` | `'left' \| 'right' \| 'alternate'` | `'left'` | Layout mode. |
| `pending` | `boolean` | `false` | Show a pulsing indicator after the last item. |
| `locale` | `Partial<TimelineLocale>` | — | Override the pending label text. |
| `class` | `string` | — | Additional CSS classes on the root element. |

### TimelineItem

| Prop | Type | Default | Description |
| --- | --- | --- | --- |
| `title` | `string` | — | Primary heading for the event. **Required.** |
| `description` | `string \| JSX.Element` | — | Secondary text below the title. |
| `time` | `string` | — | Timestamp or date displayed beside the title. |
| `icon` | `JSX.Element` | — | Custom node icon. Defaults to a coloured dot. |
| `color` | `string` | — | Tailwind text-colour class for the node (e.g. `'text-blue-500'`). |
| `content` | `JSX.Element` | — | Extra content rendered below title/description. |

### TimelineLocale

| Key | Type | Default (`en-US`) |
| --- | --- | --- |
| `pending` | `string` | `'Loading...'` |

Override via the `locale` prop or globally through `ConfigProvider`.

## Exports

| Name | Description |
| --- | --- |
| `Timeline` | Timeline component. |
| `TimelineProps` | Props type for `Timeline`. |
| `TimelineItem` | Item descriptor type. |
| `TimelineMode` | `'left' \| 'right' \| 'alternate'` union type. |
